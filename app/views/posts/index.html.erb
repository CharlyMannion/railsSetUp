<%= link_to "Add a new post", new_post_path %>

<ul class="posts">
  <% @posts.each do |post| %>
  <li>
    <%= post.title %>
    <%= link_to 'Destroy', post_path(post),
        method: :delete,
        data: { confirm: 'Are you sure?' } %>
  </li>
  <% end %>
</ul>
